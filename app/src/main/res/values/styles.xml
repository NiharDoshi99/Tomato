<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CollapsedAppbar" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="ExpandedAppbar" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textStyle">normal</item>
    </style>


    <!-- Base application theme. -->
    <style name="AppTheme1" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Styling for the centering the {@link TextView}s overlaid on the book -->
    <style name="book_info_centering">
        <item name="android:layout_marginLeft">@dimen/book_info_centering_left</item>
        <item name="android:layout_marginRight">@dimen/book_info_centering_right</item>
    </style>

    <!-- Styling for the book's title {@link TextView} -->
    <style name="book_title_text" parent="book_info_centering">
        <item name="android:typeface">serif</item>
        <item name="android:textSize">@dimen/book_title_text_size</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Styling for the book's author {@link TextView} -->
    <style name="book_author_text">
        <item name="android:typeface">monospace</item>
        <item name="android:textSize">@dimen/book_secondary_info</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
    


</resources>